    # Draw everything
    def draw(self):
        # Clear the screen
        self.screen.fill(black)

        self.screen.blit(EuromastImg, (65, 0))

        # Draw the lines
        self.Line.draw(self.screen)

        # Draw the players
        self.Player1.draw(self.screen)
        self.Player2.draw(self.screen)

        self.Player.drawturn(self.screen)

        # Draw the turn indicator
        self.score_text = self.font.render("Beurt van:", 10, white)
        self.screen.blit(self.score_text, (390, 270))

        # Flip the screen
        pygame.display.flip()

    def drawcorrect(self):
        # Clear the screen
        self.screen.fill((black))

        self.screen.blit(EuromastImg, (65, 0))

        # Draw the lines
        self.Line.draw(self.screen)

        # Draw the players
        self.Player1.draw(self.screen)
        self.Player2.draw(self.screen)

        self.Player.drawturn(self.screen)

        # Draw the turn indicator
        self.score_text = self.font.render("Beurt van:", 10, white)
        self.screen.blit(self.score_text, (390, 270))

        self.answer_text = self.font.render("Correct!", 10, black)
        self.screen.blit(self.answer_text, (250, 30))

        pygame.display.flip()

    def drawwrong(self):

        # Clear the screen
        self.screen.fill((0, 0, 0))

        self.screen.blit(EuromastImg, (65, 0))

        # Draw the lines
        self.Line.draw(self.screen)

        # Draw the players
        self.Player1.draw(self.screen)
        self.Player2.draw(self.screen)

        self.Player.drawturn(self.screen)

        # Draw the turn indicator
        self.score_text = self.font.render("Beurt van:", 10, white)
        self.screen.blit(self.score_text, (390, 270))

        self.answer_text = self.font.render("Fout!", 10, black)
        self.screen.blit(self.answer_text, (250, 30))
        pygame.display.flip()